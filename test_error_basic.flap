// Test basic error Result behavior
x := 10 / 0
println("After division by zero")

// Check if x is NaN using comparison
// NaN != NaN so this should work
y := x == x
y {
    0 => println("x is NaN (error)")
    1 => println("x is not NaN")
    ~> println("unexpected")
}
