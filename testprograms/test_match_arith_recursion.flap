// Test recursion with match and arithmetic on both params
countdown := (n, acc) => n <= 1 {
  -> acc
  ~> {
    println(n)
    println(acc)
    me(n - 1, acc * n)
  }
}

result := countdown(5.0, 1.0)
printf("Result: %.0f\n", result)
