// Test to dump the string map data
s := "hi"

// Load first character (should be 'h' = 104)
char0 := s[0]
printf("First char code = %v (should be 104 for 'h')\n", char0)

// Load second character (should be 'i' = 105)
char1 := s[1]
printf("Second char code = %v (should be 105 for 'i')\n", char1)
