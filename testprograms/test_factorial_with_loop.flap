// Test factorial with loop - EXACTLY like original
factorial := (n, acc) => n <= 1 {
    -> acc
    ~> me(n - 1, acc * n)
}

printf("Factorials:\n")
@ i in 1..<6 {
    result := factorial(i, 1)
    printf("%v! = %v\n", i, result)
}
