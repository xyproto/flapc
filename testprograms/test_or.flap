// Test logical OR operator
// or returns 1.0 if either operand is non-zero, 0.0 otherwise

// Both non-zero
result1 := 5 or 10
println(result1)

// First zero, second non-zero
result2 := 0 or 10
println(result2)

// First non-zero, second zero
result3 := 5 or 0
println(result3)

// Both zero
result4 := 0 or 0
println(result4)

// Negative numbers (non-zero)
result5 := -5 or -10
println(result5)
