// Parallel sum example using reducer
// Demonstrates parallel loop with reduction

// Sum numbers from 1 to 10000 in parallel
sum := @@ i in 1..=10000 { i } | a,b | { a + b }

printf("Sum of 1 to 10000 = %.0f\n", sum)

// Expected result: 50005000 (n*(n+1)/2)
expected := 10000 * (10000 + 1) / 2
printf("Expected: %.0f\n", expected)

// Verify correctness
sum == expected {
    println("Success: Parallel sum is correct!")
} else {
    println("Error: Parallel sum is incorrect!")
}