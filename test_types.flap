// Test: Are strings and numbers really the same?

// Test 1: Direct assignment
num := 42
str := "hello"
println(num)  // Works
println(str)  // Works

// Test 2: In match with numbers
result1 := 0 {
    0 -> 42
    ~> 99
}
println(result1)  // Works: 42

// Test 3: In match with strings
result2 := 0 {
    0 -> "hello"
    ~> "goodbye"
}
println(result2)  // Fails: garbage
