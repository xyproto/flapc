list := [10, 20, 30, 40]
printf("Before tail: list[0]=%f, list[1]=%f, #list=%f\n", list[0], list[1], #list)
rest := _list
printf("After tail: rest pointer as number = %f\n", rest)
printf("After tail: #rest = %f\n", #rest)
printf("After tail: rest[0] = %f\n", rest[0])
printf("After tail: rest[1] = %f\n", rest[1])
