// parallel_test_delay.flap - Test with operations between parallel and index
values = [1.0, 2.0, 3.0]

doubled = values || (x) -> x * 2

// Do some other operations
dummy = 5.0 + 3.0

println(doubled[0])
println(doubled[1])
println(doubled[2])

