// parallel_test_const.flap - Test parallel with constant lambda
values = [1.0, 2.0, 3.0]

// Map operation: return constant
result = values || (x) -> 99.0

println(result[0])
println(result[1])
println(result[2])

