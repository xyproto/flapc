// parallel_test_debug.flap - Debug parallel operator
values = [5.0, 7.0, 11.0]

// Map operation: double each value
doubled = values || (x) -> x * 2

println(doubled[0])
println(doubled[1])
println(doubled[2])

exit(0)
