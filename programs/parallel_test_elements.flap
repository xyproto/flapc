// parallel_test_elements.flap - Test parallel operator with element access
values = [1.0, 2.0, 3.0]

// Map operation: double each value
doubled = values || (x) -> x * 2

// Print individual elements
println(doubled[0])
println(doubled[1])
println(doubled[2])

