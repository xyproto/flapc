// parallel_test_direct.flap - Store result in variable and print
values = [1.0, 2.0, 3.0]

doubled = values || (x) -> x * 2

first = doubled[0]
second = doubled[1]
third = doubled[2]

println(first)
println(second)
println(third)

