// parallel_test_four.flap - Test with 4 elements
values = [1.0, 2.0, 3.0, 4.0]

doubled = values || (x) -> x * 2

println(doubled[0])
println(doubled[1])
println(doubled[2])
println(doubled[3])

exit(0)
