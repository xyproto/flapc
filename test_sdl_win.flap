import sdl3 as sdl

printf("About to init SDL3...\n")
result := sdl.SDL_Init(sdl.SDL_INIT_VIDEO)
printf("SDL_Init returned: %d\n", result)

result or! {
    printf("SDL_Init failed!\n")
    exit(1)
}

printf("SDL_Init succeeded!\n")
sdl.SDL_Quit()
printf("Done!\n")
