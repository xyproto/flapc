// Test just the malloc part
list := [100, 200]
// This should trigger tail operator which calls malloc
rest := _list
// If we get here without segfault, malloc worked
println("malloc worked")
println(#rest)
// Now try to read
val := rest[0]
println("read worked")
println(val)
